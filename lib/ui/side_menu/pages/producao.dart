import 'package:flutter/material.dart';

class Producao extends StatelessWidget {
  const Producao({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Producao"));
  }
}