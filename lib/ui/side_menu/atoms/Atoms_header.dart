import 'package:flutter/material.dart';

class AtomsHeader extends StatelessWidget {
  const AtomsHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      "assets/images/logo.jpg",
      width: 50,
      height: 50,
    );
  }
}
